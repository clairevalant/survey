import React from 'react';

function Question(props) {
    const q = props.currentQ;
    // console.log(q)
    
    return (
        // <h2>{q}</h2>
        <h2>hellor</h2>
    );
};

export default Question;